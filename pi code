import paho.mqtt.client as mqtt
import time

# MQTT Broker settings
broker_address = "192.168.100.200"  # IP address of your Raspberry Pi
broker_port = 1883

# Variables for data storage
pm03um = None
pm05um = None
pm10um = None
pm25um = None

# Callback function for when a message is received
def on_message(client, userdata, message):
    global pm03um, pm05um, pm10um, pm25um
    payload = message.payload.decode()

    if message.topic == "AQI/3um":
        pm03um = int(payload)  # Convert payload to int
    elif message.topic == "AQI/5um":
        pm05um = int(payload)  # Convert payload to int
    elif message.topic == "AQI/10um":
        pm10um = int(payload)  # Convert payload to int
    elif message.topic == "AQI/25um":
        pm25um = int(payload)  # Convert payload to int

# Create a new MQTT client instance
client = mqtt.Client()

# Assign the callback function to the client
client.on_message = on_message

# Connect to the MQTT broker
client.connect(broker_address, broker_port)

# Subscribe to multiple topics
client.subscribe("AQI/3um")
client.subscribe("AQI/5um")
client.subscribe("AQI/10um")
client.subscribe("AQI/25um")

# Start the loop to process incoming messages
client.loop_start()

try:
    while True:
        # Print received data for each PM level
        print("PM 0.3 um:", pm03um)
        print("PM 0.5 um:", pm05um)
        print("PM 1.0 um:", pm10um)
        print("PM 2.5 um:", pm25um)
        time.sleep(1)  # Adjust the sleep time as necessary
except KeyboardInterrupt:
    print("Exiting...")
finally:
    client.loop_stop()  # Stop the loop
    client.disconnect()  # Disconnect from the broker
