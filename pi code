import paho.mqtt.client as mqtt

# MQTT Broker settings
broker_address = "192.168.100.12"  # IP address of your Raspberry Pi
broker_port = 1883

# Callback function for when a message is received
def on_message(client, userdata, message):
    # Print the topic and message received
    print(f"Message received on topic {message.topic}: {message.payload.decode()}")

# Create a new MQTT client instance
client = mqtt.Client()

# Assign the callback function to the client
client.on_message = on_message

# Connect to the MQTT broker
client.connect(broker_address, broker_port)

# Subscribe to multiple topics
client.subscribe("AQI/3um")
client.subscribe("AQI/5um")
client.subscribe("AQI/10um")
client.subscribe("AQI/25um")

# Start the loop to process incoming messages
client.loop_start()

try:
    # Keep the script running
    while True:
        pass  # You can add any other code you want to run here

except KeyboardInterrupt:
    print("Exiting...")
    client.loop_stop()  # Stop the loop
    client.disconnect()  # Disconnect from the broker
